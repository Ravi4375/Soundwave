package com.soundwave.repository;

/**
 * Legacy placeholder kept to avoid stale IDE references after repository interfaces were split
 * into their own files.
 */
public final class Repositories {
    private Repositories() {
    }
}
